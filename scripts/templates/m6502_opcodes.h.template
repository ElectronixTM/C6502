#ifndef M6502_OPCODES_H
#define M6502_OPCODES_H

/**
 * В этом файле описываются все опкоды микроконтроллера. Все опкоды
 * могут отличаться по режиму адресации, что тоже следует учитывать
 */

/**
 * Все возможные режимы адрессации микроконтроллера MOS 6502
 */
enum m6502_AddressingModes
{
  AM_A,
  AM_ABS,
  AM_ABS_X,
  AM_ABS_Y,
  AM_IMM,
  AM_IMPL,
  AM_IND,
  AM_X_IND,
  AM_IND_Y,
  AM_REL,
  AM_ZPG,
  AM_ZPG_X,
  AM_ZPG_Y
};

/***/
enum m6502_OpCode{
  {{OPCODES}}
};

/**
 * В описании опкодов хранится информация о номере опкода,
 * адрессации операндов, длине команды целиком, количество
 * циклов, необходимых для исполнения
 */
<<<<<<< HEAD
struct m6502_OpcodeDesc
=======
struct m6502_OpCodeDesc
>>>>>>> feature/scripts/optable-generation
{
  m6502_OpCode opcode; /** код операции из списка известрных эмулятору.
                           Остальные опкоды не эмулируются и фактически
                           становятся NOP'ами */
  uint8_t instrsize; /**< размер инструкции вместе с опкодом и операндами */
  uint8_t requiredcycles; /** Количество сигналов clock, необходимых для
                              выполнения инструкции*/
  m6502_AddressingModes addressmode; /** режим адресации инструкции*/
};

const struct m6502_OpcodeDesc* m6502_fetch_opcode_desc(uint_8t);

#endif
